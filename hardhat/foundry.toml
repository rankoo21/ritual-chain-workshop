[profile.default]
src = "contracts"
out = "out"
libs = ["lib"]
test = "test"
solc = "0.8.24"
optimizer = true
optimizer_runs = 200
evm_version = "cancun"

# Run unit/fuzz tests:   forge test -vvv
# Run fork tests:        forge test --match-contract RitualForkBounty --fork-url <RITUAL_RPC_URL> -vvv
